# 2026-06-08

## 한국어 복음 AI 프로젝트 감사 계속

### 남은 작업 항목 식별
이전 세션에서 16+ 감사 항목을 처리함. 남은 하위 우선순위 항목:
- CORS 정 refinement
- 스트림 파이프라인 G-1~G-5 추가
- User UI 스트리밍 모드

### 작업 시작
	continue 명령으로 남은 항목들을 처리하기 시작함.

### 작업 완료 (6건)

#### 1. `/chat/stream` 엔드포인트에 G-1~G-5 추가 (Task #17)
- **G-1 (입력 분류)**: `classify_input()` 추가, 병렬 실행으로 성능 유지
- **G-2 (모호한 질문 처리)**: `classification.clarity == Clarity.ambiguous` 시 즉시 재질문 반환 (JSONResponse)
- **G-3 (영적 교정 블록)**: `build_correction_block()` 호출, system_prompt 앞에 prepends
- **G-5 (응답 구조 가이드)**: `build_response_structure_block()` 호출, system_prompt 뒤에 append
- G-4.5/G-4.6 (아첨 필터+재생성)은 이미 구현되어 있었음 (스트리밍 완료 후 실행)

수정 파일: `backend/app/api/chat.py` (lines 499-637 → 499-645)

#### 2. CORS 설정 정비 (Task #16)
- `allow_methods`: `["*"]` → `["GET", "POST", "OPTIONS"]` (명시적 제한)
- `allow_headers`: `["*", "X-Request-ID"]` → `["X-Request-ID", "Content-Type", "Authorization"]` (필요한 헤더만)
- `expose_headers`: `["X-Request-ID"]` 추가 (클라이언트가 추적 ID 읽기 가능)

수정 파일: `backend/app/main.py` (lines 59-66)

#### 3. User UI 스트리밍 모드 추가 (Task #18)
- **세션 스테이트**: `streaming_mode: False` 추가
- **상단 바 토글**: 🟢/⚪ 버튼으로 스트리밍 켜기/끄기
- **스트리밍 로직**: `httpx.stream()` 사용, `r.iter_text()` 로 청크 수신, `ph.markdown()` 로 실시간 업데이트
- **비스트리밍 유지**: 기존 `/chat` 엔드포인트 사용 (호환성 유지)

수정 파일: `user/app.py`

#### 4. 대용량 파일 업로드 제한 추가 (Task #19)
- **config.py**: `max_upload_size_mb: int = 100` 설정 추가
- **/upload 엔드포인트**: 파일 크기 검증 추가 (413 에러 반환)
- **/upload-async 엔드포인트**: 파일 크기 검증 추가

수정 파일: `backend/app/config.py`, `backend/app/api/documents.py`

#### 5. 파일 처리 미리보기 모드 추가 (Task #20)
- **새 엔드포인트**: `GET /documents/{doc_id}/versions/{ver_id}/preview`
- **반환 내용**:
  - 전체 추출 텍스트 (`full_text`)
  - 품질 검사 결과 (`warnings`, `quality_score`)
  - 중복 감지 결과 (`dedup_hits`)
  - 청크 미리보기 (`chunk_preview` - 처음 10개)

수정 파일: `backend/app/api/documents.py`

### 기술적 결정
1. **스트림 엔드포인트 G-2 처리**: 모호한 질문은 스트리밍 없이 즉시 `JSONResponse` 반환 (사용자 경험 개선)
2. **CORS `allow_credentials=False` 유지**: 쿠키 인증 안 함 (Bearer 토큰 사용)
3. **UI 스트리밍 토글**: 사용자가 원할 때만 스트리밍 (비용/속도 조절 가능)
4. **미리보기 엔드포인트**: 발행 전 전체 텍스트와 품질 검사 결과를 미리 볼 수 있음

### 남은 작업
- 통합 테스트 (스트리밍 파이프라인 검증) - Task #21
- Git 커밋 및 푸시
- 배포 및 프로덕션 검증
