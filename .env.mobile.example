# 移动端(Web/APK) 构建时注入的环境变量 —— 由 scripts/build-config.js 读取
# 复制为 .env.mobile 并填入真实值（.env.mobile 已被 .gitignore 忽略，不会入库）
#
# 市场策略（2026-07-31 用户决策）：
#   - 后端部署：香港（大陆用户直连可达，免备案，低延迟）
#   - 分发：香港自有域名直链（不上 Google Play / 国内商店）
#   - 支付：PortOne V2 + 微信支付/支付宝 channel（PortOne 控制台配置，客户端零改动）
#   - 中国 APK：零 Google 依赖（字体系统化、GIS SDK 仅海外构建注入）

# ── 默认（android/web 未指定 --market 时使用）──
# 香港后端 API 基址（大陆可达）。例：https://api.flowai.hk
ANDROID_API_BASE=https://api.flowai.hk
WEB_API_BASE=https://api.flowai.hk

# ── 按市场细分（--market=cn|hk|global 时优先）──
# 中国大陆纯离线构建（彻底无 Google）：api 用国内可达域名
CN_API_BASE=https://api.flowai.hk
# 香港/海外构建
GLOBAL_API_BASE=https://api.flowai.hk
HK_API_BASE=https://api.flowai.hk
