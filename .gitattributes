# Hugging Face Spaces - LFS settings (chroma_db는 push 안하지만 안전망)
*.bin filter=lfs diff=lfs merge=lfs -text
*.sqlite3 filter=lfs diff=lfs merge=lfs -text

# 텍스트 파일은 LF
*.py text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.toml text eol=lf
