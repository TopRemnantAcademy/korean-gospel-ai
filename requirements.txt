#  RAG v3 - Production-ready ingestion + lifecycle
# Python 3.10 / 3.11 / 3.12

# ----- Web -----
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.31  # SECURITY: 14 OSV advisories @0.0.20 (ReDoS/DoS), fixed 0.0.31
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1

# ----- LLM -----
google-genai==0.3.0
openai==1.59.6
anthropic>=0.40.0
cohere>=5.0.0

# ----- Embeddings / Models -----
sentence-transformers==6.0.1  # CVE-2026-68770 (trust_remote_code=False 우회 RCE, 영향 ≤5.5.1 → 6.0.1 고정)
transformers>=5.0.0,<6.0.0  # st 6.0.1 요구 transformers>=5.0.0 (<6.0.0) — 4.47.1 은 CVE 패치와 비호환

# ----- Vector DB -----
qdrant-client==1.12.2
fastembed==0.8.0  # SECURITY: bump so Pillow 12.3.0 (CVE fixes) is allowed; 0.5/0.6/0.7 cap pillow<12

# ----- Database (SQLite -> Postgres later) -----
sqlalchemy==2.0.36
alembic==1.14.0

# ----- Korean text -----
kss==6.0.5

# ----- Tracing -----
langfuse==2.57.0

# ----- Document loaders -----
pypdf==6.16.1  # SECURITY: 77 OSV advisories @5.1.0, fixed 6.16.1 (API: PdfReader(BytesIO) unchanged)
python-docx==1.1.2
pymupdf==1.25.1
Pillow==12.3.0  # SECURITY: 34 OSV advisories @10.4.0, fixed 12.3.0 (transitive only in this repo)

# ----- Admin UI -----
streamlit==1.54.0  # SECURITY: 4 advisories @1.41.1 (PYSEC-2026-212/2285), fixed 1.54.0
pandas==2.2.3
plotly==5.24.1
streamlit-pdf-viewer==0.0.21

# ----- Utils -----
python-dotenv==1.2.2  # SECURITY: 2 advisories @1.0.1 (PYSEC-2026-2270), fixed 1.2.2
PyYAML==6.0.2
numpy==2.0.2

# ----- Push notifications (선택 — APK 푸시 알림 활성화 시 설치) -----
# pywebpush==2.0.3   # Web Push (VAPID) — PWA Service Worker 푸시
# apscheduler==3.10.4  # 매일 말씀 발송 스케줄러
# firebase-admin==6.6.0  # FCM 토큰 발송 (Android/iOS 네이티브)
